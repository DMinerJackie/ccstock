INCLUDE_DIRECTORIES(/usr/include) 
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET( CMAKE_BUILD_TYPE Debug )

ADD_DEFINITIONS (
	-std=c++11
)

ADD_EXECUTABLE(test_ev test_ev.cpp)

TARGET_LINK_LIBRARIES(test_ev
    ev
)
